#include<bits/stdc++.h> 
using namespace std;




int main() {
    double num ;
    // getline(cin,str); 
    // stringstream ss(str);
    // vector<int> arr; int nums;

    // while(ss>>nums) arr.push_back(nums);

    //  for(auto x:arr) cout<<x<<"*"; 

    cin>>num;
    cout<<fixed<<setprecision(2)<<num;

     return 0;
} 
